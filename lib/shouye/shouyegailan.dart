import 'package:flutter/material.dart';

class shouyeganlan extends StatelessWidget {
  String title;
  shouyeganlan({Key? key, required this.title}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
